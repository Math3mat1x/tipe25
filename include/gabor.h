#ifndef GABOR_H
#define GABOR_H
#include "ppm.h"

float** create_gabor_kernel(int size, float angle, float frequency, float sigma_x, float sigma_y);
float** create_gabor_kernel(int size, float angle, float frequency, float sigma_x, float sigma_y);


#endif /* GABOR_H */
